# Copy to .env.local and fill in. These are the PUBLIC client vars (safe to ship).
# Your Anthropic key is NOT here — it lives only as a Supabase secret on the server.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
